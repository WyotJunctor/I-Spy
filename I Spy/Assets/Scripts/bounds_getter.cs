using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class bounds_getter : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        new GameObject();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
